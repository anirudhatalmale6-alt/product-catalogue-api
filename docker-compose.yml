services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_KEY=demo-api-key-2024
      - DATABASE_URL=sqlite:///./products.db
    volumes:
      - db-data:/app
    restart: unless-stopped

volumes:
  db-data:
